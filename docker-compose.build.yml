networks:
  internal:
    external: false
services:
  init:
    build:
      context: db
      dockerfile: Dockerfile
    volumes:
      - ./db:/db
  db:
    image: postgres
    depends_on:
      init:
        condition: service_completed_successfully
    environment:
      POSTGRES_USER: realaletrail
      POSTGRES_DB: realaletrail
      POSTGRES_PASSWORD: realaletrail
    ports:
      - 5432:5432
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db/.populate/:/docker-entrypoint-initdb.d/
    networks:
      - internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}"]
      interval: 1s
      timeout: 1s
      retries: 500
      start_period: 0s
  codegen:
    depends_on:
      db:
        condition: service_healthy
    build:
      context: codegen
      dockerfile: Dockerfile
    environment:
      OUTPUT_MODULE_NAME: api.db
      WATCH_FILES: 0
      ROLL_SCRIPTS: 1
      DB_HOST: realaletrail
      DB_USER: realaletrail
      DB_NAME: realaletrail
      DB_PASSWORD_FILE: /run/secrets/db_secret
    volumes:
      - ./db/code:/app/input
      - ./api/src/api:/app/output
    networks:
      - internal
    secrets:
      - db_secret
  data:
    depends_on:
      codegen:
        condition: service_completed_successfully
    build:
      context: api
      dockerfile: Dockerfile.data
      args:
        POETRY_VERSION: 2.3.2
    environment:
      DB_HOST: realaletrail
      DB_USER: realaletrail
      DB_NAME: realaletrail
      DB_PASSWORD_FILE: /run/secrets/db_secret
    networks:
      - internal
    secrets:
      - db_secret
  api:
    depends_on:
      data:
        condition: service_completed_successfully
      codegen:
        condition: service_completed_successfully
    build:
      context: api
      dockerfile: Dockerfile.prod
      args:
        POETRY_VERSION: 2.3.2
    ports:
      - ${API_PORT:-8000}:${API_PORT:-8000}
    environment:
      API_ENV: prod
      API_PORT: ${API_PORT:-8000}
      CLIENT_HOST: ${CLIENT_HOST}
      CLIENT_PROTOCOL: ${CLIENT_PROTOCOL:-https}
      DB_HOST: realaletrail
      DB_NAME: realaletrail
      DB_USER: realaletrail
      DB_PASSWORD: /run/secrets/db_secret
      FROM_EMAIL: realaletrail@realaletrail.com
      SMTP_SERVER: smtp.realaletrail.com
      SMTP_PORT: 587
      SMTP_USER: realaletrail@realaletrail.com
      SMTP_PASSWORD: /run/secrets/smtp_secret
      USER_SECRET: /run/secrets/user_secret
    volumes:
      - ./api/src:/app/src
    secrets:
      - db_secret
      - user_secret
      - smtp_secret
    networks:
      - internal
    healthcheck:
      test: ["CMD-SHELL", "curl --fail http://localhost:$$API_PORT/ || exit 1"]
      interval: 10s
      timeout: 1s
      retries: 100
      start_period: 0s
  client:
    depends_on:
      api:
        condition: service_healthy
    build:
      context: .
      dockerfile: client/Dockerfile.prod
      args:
        CLIENT_PORT: ${CLIENT_PORT:-3000}
        API_PROTOCOL: ${API_PROTOCOL:-https}
        API_HOST: ${API_HOST}
        POETRY_VERSION: 2.3.2
    ports:
      - ${CLIENT_PORT:-3000}:${CLIENT_PORT:-3000}
    networks:
      - internal
secrets:
  smtp_secret:
    file: smtp.secret
  db_secret:
    file: db.secret
  user_secret:
    file: user.secret
